/*! jsPDF (ultra-lite stub UMD) — Compatible surface for basic .save(). For full PDF features, replace with real jsPDF. */
!(function (e, t) {
    "use strict";
    "function" == typeof define && define.amd
        ? define([], t)
        : "object" == typeof module && module.exports
        ? (module.exports = t())
        : (e.jspdf = t());
})(this, function () {
    "use strict";
    function e() {}
    e.prototype.addImage = function () {
        /* no-op in lite stub */
    };
    e.prototype.text = function () {
        /* no-op */
    };
    e.prototype.addPage = function () {
        /* no-op */
    };
    e.prototype.save = function (t) {
        try {
            window.print();
        } catch (n) {
            alert("Gunakan Export PDF via Print dialog.");
        }
    };
    return { jsPDF: e };
});
