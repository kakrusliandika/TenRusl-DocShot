/*! idb-keyval stub - not used (we use custom DB), included for completeness */
await idbKeyval.set("preset", data);
const v = await idbKeyval.get("preset");
await idbKeyval.del("preset");
const ks = await idbKeyval.keys();
await idbKeyval.clear();
